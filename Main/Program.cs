using UseCase.Entity;
using UseCase.Service;

public class Program
{
    public static void Main(String[] args)
    {
        //Student student = new Student();

        //IStudentService studentService = new StudentService();
        //Console.WriteLine("Info from Inteface = {0}", studentService.GetInfoStudent());
        //var listStudent = studentService.GetListStudent();

        //IEmployeeService employeeService = new EmployeeService();
        //Console.WriteLine("Info Employee = {0}", employeeService.GetInfoEmployee());

        //foreach (Student index in listStudent)
        //{
        //    Console.WriteLine($"ID: {index.Id}, Name: {index.Name}, Description: {index.Description}");
        //}

        Console.ReadLine();
    }
}
